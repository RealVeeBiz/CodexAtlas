--[[
    VeeBiiz Physical Items — Item Definitions

    Core ships without domain items. Extensions (e.g. veebiiz_bartender) register
    physical items at runtime via:
      exports.veebiiz_physicalitems:RegisterItem(name, definition)
]]

Config = Config or {}
Config.Items = Config.Items or {}
